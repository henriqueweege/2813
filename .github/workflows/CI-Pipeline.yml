# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET


on:
  push:
    branches: [ "main" ]

jobs:
  build:
    
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 7.0.x
    - name: Restore dependencies
      run: dotnet restore ./Exercicio/DependencyRoomBooking.sln
    - name: Build
      run: dotnet build --no-restore  ./Exercicio/DependencyRoomBooking.sln
    - name: Repositories Tests
      run: dotnet test --no-build --verbosity normal ./Exercicio/Tests/Domain_Tests/Repositories_UnitTests.csproj
    - name: Converters Tests
      run: dotnet test --no-build --verbosity normal ./Exercicio/Tests/Domain_Tests/Converters_UnitTests.csproj
    - name: Handler Tests
      run: dotnet test --no-build --verbosity normal ./Exercicio/Tests/Domain_Tests/Handler_UnitTests.csproj
    - name: Fake Repositories Tests
      run: dotnet test --no-build --verbosity normal ./Exercicio/Tests/TestsTools_Tests/FakeRepositories_UnitTests.csproj
